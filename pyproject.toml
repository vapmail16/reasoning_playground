[tool.poetry]
name = "reasoning-playground"
version = "0.1.0"
description = "A reasoning explorer using GPT and prompt-based reasoning types"
authors = ["Vikkas Arun Pareek <vapmail16@gmail.com"]
readme = "README.md"
package-mode = false


[tool.poetry.dependencies]
python = "^3.10"
openai = "^1.10"
streamlit = "^1.30"
pandas = "^2.2"
python-dotenv = "^1.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
